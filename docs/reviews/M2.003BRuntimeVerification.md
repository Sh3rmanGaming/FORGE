# FORGE M2.003B Runtime Verification

**Status:** Verified  
**Milestone:** M2.003B – ForgeOS Registration Foundation  
**Verification Date:** 2026-08-05

## Purpose

This record captures the Farming Simulator 25 runtime evidence for the
M2.003B coordinator-only ForgeOS Registration Foundation.

It distinguishes:

1. Coordinator Capability Verification using explicit test participants.
2. Production Integration Verification without concrete registry
   participants.

## Runtime Environment

- GIANTS Engine Runtime: `10.0.0 (47901)`, 64-bit Steam
- GIANTS Engine build date: `Jul 29 2026`
- Farming Simulator 25 version: `1.21.1.0`
- Mod descriptor version: `111`
- FORGE mod version: `0.4.1.0`
- Deployed FORGE mod hash: `bf7204950932778d625ca0ecca851042`
- Runtime platform: Steam

No separate Farming Simulator Build ID is present in the supplied log. Runtime
build identification is limited to the GIANTS Runtime build number, game
version, FORGE version, and deployed mod hash.

## Evidence Source

- Log:
  `C:\Users\sheri\OneDrive\Documents\My Games\FarmingSimulator2025\log.txt`
- Log last modified: `2026-08-05T22:30:00.2410774+08:00`
- FORGE mod discovery: line 221
- FORGE mod loading: line 556
- Relevant M2.003B test interval: lines 1330–1348
- Relevant production startup: lines 1349–1351
- Relevant production shutdown: lines 1670–1671

Line numbers refer to the supplied log as inspected on the verification date.

## Verified Architecture Boundary

M2.003B implements registration coordination only.

The coordinator requires explicit participants for:

- Device Registry;
- Device Host Registry; and
- App Registry.

Concrete production participants remain deferred to their dedicated
milestones. Consequently:

- the test lifecycle can complete startup with explicit test participants;
- production ForgeOS opens registration and waits for concrete participants;
  and
- production does not yet enter `REGISTRATION_FROZEN` or `RUNTIME_ACTIVE`.

This distinction is intentional and architecturally correct.

## Coordinator Capability Verification

### Coordinator component harness

- Line 1330: ForgeOS Registration Coordinator test harness started.
- Line 1331: the test lifecycle opened registration.
- Line 1332: coordinated registration reached registration-frozen.
- Lines 1333–1337: failure handling, cleanup, and restarted test lifecycles
  reached clean stopped states.
- Line 1338: ForgeOS Registration Coordinator test harness passed.

The passing harness verifies:

- explicit participant-role installation;
- required-role completeness;
- deterministic validation and freeze coordination;
- late-participant rejection;
- validation and freeze failure handling;
- participant cleanup; and
- restart behaviour.

### Registration lifecycle integration harness

- Line 1339: ForgeOS Registration Lifecycle integration test started.
- Line 1340: the first lifecycle opened registration.
- Line 1341: the first lifecycle reached registration-frozen.
- Line 1342: the first lifecycle reached runtime-active.
- Line 1343: the first lifecycle stopped.
- Line 1344: the restarted lifecycle opened registration.
- Line 1345: the restarted lifecycle reached registration-frozen.
- Line 1346: the restarted lifecycle reached runtime-active.
- Line 1347: the restarted lifecycle stopped.
- Line 1348: ForgeOS Registration Lifecycle integration test passed.

The integration harness observes and asserts the
`REGISTRATION_FROZEN`, `STARTED`, and `STOPPED` events and their approved
payloads. The runtime-active diagnostics followed by the passing harness
therefore provide evidence that `STARTED` was published successfully for both
the initial and restarted test lifecycles.

Coordinator capability to validate, freeze, enter `RUNTIME_ACTIVE`, restart,
and shut down is Verified.

## Production Integration Verification

### Production startup

- Line 1349: production ForgeOS opened registration.
- Line 1350: FORGE persistence loaded successfully from `savegame2`.
- Line 1351: Engine startup completed.

No production registration-frozen, runtime-active, or started diagnostic
appears between production registration opening and shutdown.

This verifies that production ForgeOS remains at `REGISTRATION_OPEN` awaiting
the three required concrete participants. It does not falsely advance through
registration freeze with incomplete participant roles.

### Production shutdown

- Line 1670: production ForgeOS entered stopped.
- Line 1671: Engine shutdown completed after ForgeOS stopped.

The Engine reported clearing zero event groups, two State Store namespaces, and
two persistence registrations.

Production integration at the coordinator-only boundary is Verified:
registration opens, Engine startup completes, ForgeOS waits safely for
participants, and shutdown completes in the correct order.

## Intentional Negative-Path Diagnostics

Line 1316 records:

```text
Rejected ForgeOS phase transition from 'initialising' to 'runtimeActive'
```

This is generated intentionally by the pre-existing ForgeOS Core negative-path
test. The Core harness passes immediately afterward at line 1317. It is not an
M2.003B failure or Lua execution error.

The log also contains deliberate error-level diagnostics from existing Logger,
Event Bus, State Store, Save Manager, XML, and persistence tests. Those
diagnostics exercise established negative paths and are followed by their
expected passing evidence.

No unexpected ForgeOS Lua source error, failed M2.003B harness, Lua call stack,
or M2.003B startup/shutdown failure appears in the relevant runtime interval.

## Verification Scope

This record verifies:

- the internal Registration Participant contract as exercised by test
  participants;
- required participant-role completeness;
- deterministic coordinator ordering;
- validation orchestration;
- registration freeze orchestration;
- shared registration phase gating;
- late-participant rejection at the coordinator boundary;
- registration-frozen and started event publication;
- coordinator restart and cleanup;
- transition to runtime-active when all explicit participants exist;
- production registration-open integration; and
- production shutdown before Engine cleanup.

## Explicitly Out of Scope

This record does not verify:

- concrete Device Registry implementation;
- concrete Device Host Registry implementation;
- concrete App Registry implementation;
- registry storage or lookup;
- duplicate definition detection;
- identifier or definition-specific validation;
- registry-specific registered events;
- production transition to `REGISTRATION_FROZEN`;
- production transition to `RUNTIME_ACTIVE`;
- production `STARTED` publication;
- device-host runtime behaviour;
- application execution;
- presentation resolution;
- availability, navigation, or notifications;
- gameplay behaviour;
- unresolved ForgeOS architecture decisions;
- M2.003 parent milestone acceptance; or
- completion of later M2 work.

Production registration freeze and runtime activation remain deferred until
all three concrete registry participants exist.

## Verification Conclusion

M2.003B is Verified within its approved coordinator-only scope.

Explicit test participants demonstrate that the coordinator can validate and
freeze all required registration sets, publish the approved lifecycle events,
enter `RUNTIME_ACTIVE`, restart, and shut down cleanly.

Production ForgeOS correctly opens registration, completes Engine integration,
waits in `REGISTRATION_OPEN` because concrete participants do not yet exist,
and reaches `STOPPED` before Engine shutdown completes.

This conclusion does not complete or freeze the parent M2.003 milestone and
does not advance any concrete registry milestone.

