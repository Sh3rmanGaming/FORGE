# FORGE M2.007 Runtime Verification

**Status:** Verified
**Milestone:** M2.007 – ForgeOS Lifecycle Service
**Verification Date:** 2026-08-08

## Purpose

This record captures the Farming Simulator 25 runtime evidence for the bounded
M2.007 Lifecycle Service capability. It distinguishes controlled
runtime-active lifecycle verification from production integration that remains
at registration-open.

## Runtime Environment

- GIANTS Engine Runtime: `10.0.0 (47901)`, 64-bit Steam
- GIANTS Engine build date: `Jul 29 2026`
- Farming Simulator 25 version: `1.21.1.0`
- Build ID: `b40785`
- Build revision: `81824`
- FORGE mod version: `0.4.1.0`
- FORGE discovery hash: `b62df33cef538257abc733b860c75104`
- Development package SHA-256:
  `8CFC7FD8A7312EE6BEBC07AEA1A71669E9E7DDD674D55774DBBB720DF8579D58`
- Runtime platform: Steam

## Evidence Source

- Log:
  `C:\Users\sheri\OneDrive\Documents\My Games\FarmingSimulator2025\log.txt`
- Log last modified: `2026-08-08 21:30:45 +08:00`
- Log SHA-256:
  `AD76B097E132E0B1000885AB1CA8047D0DF4CD16F19E66C8B2A6652D8A4271E1`
- FORGE discovery and loading: lines 221 and 556–557
- Development suite: lines 1240–1441
- Production startup: lines 1442–1444
- Production save and shutdown: lines 1760 and 1763–1764

Line numbers refer to the preserved log inspected on the verification date.
The FS25 soft reset after line 1764 followed the verified normal shutdown.

## Development Bootstrap and Regression Suite

- Line 557: the established Development Verification Bootstrap enabled the
  complete development suite.
- Line 1240: the development suite started.
- Lines 1241–1418: all retained M2.003–M2.006 harnesses passed.
- Line 1441: the complete development suite passed.

The bootstrap retains its historical M2.004 diagnostic wording. Its explicit
load-order entry enabled this development-verification build and does not make
the bootstrap part of the M2.007 production contract.

## Lifecycle Service Component Verification

- Line 1419: App Lifecycle Service harness started.
- Lines 1420–1422: the controlled lifecycle reached registration-frozen and
  runtime-active.
- Line 1423: the intentional requested-app callback failure was reported.
- Lines 1424–1429: intentional lifecycle-listener failures were isolated by
  Event Bus diagnostics.
- Line 1430: the controlled lifecycle reached stopped.
- Line 1431: the component harness passed.

The passing component harness verifies:

- invalid identifiers, runtime gating, and missing device/app results;
- Presentation Resolver eligibility before open;
- enabled-policy rejection;
- valid and invalid lifecycle transitions;
- idempotence without callbacks or events;
- lifecycle-state and active-app queries;
- one active app per device;
- displacement to background with `BACKGROUND_APPS` and to closed without it;
- displaced callback and completed-event ordering;
- detached local-player callback context;
- callback failure without ForgeOS lifecycle-state or event commit;
- first-callback success followed by second-callback failure while preserving
  authoritative pre-operation ForgeOS state;
- service-local re-entrant operation rejection;
- listener-failure isolation after committed lifecycle state;
- lifecycle event identity and payload shape;
- cleanup, restart, and non-persistence of runtime lifecycle state; and
- the active-app invariant across controlled failure paths.

The callback-failure guarantee covers ForgeOS lifecycle state, active-app
ownership, and lifecycle completion events. The test does not assert rollback
of arbitrary application-owned callback side effects.

## App Registry Callback Access

The passing component harness also verifies that the internal
`getLifecycleCallback()` accessor:

- permits only `onOpen`, `onActivate`, `onBackground`, and `onClose`;
- returns nil for `onRegister`, unknown callback names, and unknown apps;
- remains usable after App Registry freeze;
- exposes no controller, provider, action handler, private definition, or
  mutable executable-reference table; and
- leaves detached public App Registry snapshots free of executable references.

## Controlled Runtime-Active Integration

- Line 1432: ForgeOS App Lifecycle integration harness started.
- Lines 1433–1435: production Device Registry, App Registry, Presentation
  Resolver, and Lifecycle Service plus one explicit Device Host test
  participant reached runtime-active.
- Line 1436: the intentional integration callback failure was reported.
- Line 1437: the controlled lifecycle stopped.
- Lines 1438–1439: restart opened registration and stopped cleanly without a
  production Host.
- Line 1440: the integration harness passed.

The passing integration harness verifies the public lifecycle facade,
presentation eligibility, enabled-policy rejection, callback execution and
failure isolation, non-reentrant rejection, active-app displacement, lifecycle
events, cleanup, shutdown, and restart through the production service stack.

## Production Integration Verification

- Line 1442: production ForgeOS opened registration.
- Line 1443: FORGE persistence loaded successfully from `savegame2`.
- Line 1444: Engine startup completed.
- Line 1760: persistence saved successfully.
- Line 1763: ForgeOS reached `STOPPED` after Lifecycle Service cleanup.
- Line 1764: Engine shutdown completed afterward.

No production registration-frozen, runtime-active, or started diagnostic
appears between production registration opening and shutdown. Production
therefore remained correctly at `REGISTRATION_OPEN` because Device Host
Registry remains deferred.

## Intentional Negative-Path Diagnostics

The log contains intentional diagnostics from the retained Logger, Event Bus,
State Store, persistence, Core, Device Registry, App Registry, and M2.007
harnesses. The M2.007-specific callback and listener errors are identified at
lines 1423–1429 and 1436. Each is followed by its named harness pass and the
suite-level pass.

No unexpected FORGE Lua error, Lua call stack, stack traceback, cascading
lifecycle contamination, active-app invariant failure, or production
startup/shutdown failure appears in the inspected cycle.

## Verification Scope

This record verifies the approved M2.007 public lifecycle facade, queries,
runtime gate, result precedence, lifecycle transitions, idempotence,
presentation eligibility, bounded enabled policy, active-app displacement,
private callback access, callback timing and failure boundary, atomic
ForgeOS-owned state commit, lifecycle events, non-reentrancy, cleanup, restart,
regression compatibility, synchronized load-order execution, and bounded
production integration.

## Explicitly Out of Scope

This record does not verify:

- a production Device Host Registry or host instance;
- production runtime activation or `STARTED`;
- availability-provider execution or general enabled-policy ownership;
- navigation, resume restoration, visibility, notifications, or rendering;
- rollback of arbitrary application callback side effects;
- cross-service transactions or re-entrancy policy;
- stable multiplayer identity or per-player persistence; or
- M2.007 milestone acceptance, maturity advancement, or contract freeze.

## Conclusion

The corrected FS25 evidence verifies the bounded M2.007 Lifecycle Service.
Controlled tests prove runtime-active lifecycle behavior and the approved
callback transaction boundary. Production correctly remains at
`REGISTRATION_OPEN`, loads persistence, completes Engine startup, clears
runtime lifecycle state during shutdown, reaches `STOPPED`, and then completes
Engine shutdown.

M2.007 is runtime verified. Milestone acceptance, roadmap advancement, and the
bounded Lifecycle Service freeze remain separate approval actions.
