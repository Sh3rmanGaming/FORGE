# FORGE M2.004 Runtime Verification

**Status:** Verified  
**Milestone:** M2.004 – ForgeOS Device Registry  
**Verification Date:** 2026-08-06

## Purpose

This record captures the Farming Simulator 25 runtime evidence for the
M2.004 Device Registry capability.

It distinguishes development-suite verification, Device Registry capability
verification, and production lifecycle integration.

## Runtime Environment

- GIANTS Engine Runtime: `10.0.0 (47901)`, 64-bit Steam
- GIANTS Engine build date: `Jul 29 2026`
- Farming Simulator 25 version: `1.21.1.0`
- Mod descriptor version: `111`
- FORGE mod version: `0.4.1.0`
- Deployed FORGE mod discovery hash: `c4b6eb0154a675dbce33c61d86a16ffd`
- Deployed package SHA-256:
  `928EE2EAFB63B23444ADD97A2B4DDE5205A8EFA657133138DD6C03B506FBEA23`
- Runtime platform: Steam

No separate Farming Simulator Build ID appears in the inspected log. Runtime
build identification is limited to the GIANTS Runtime build number, game
version, mod descriptor version, FORGE version, discovered mod hash, and
deployed package SHA-256.

## Evidence Source

- Log:
  `C:\Users\sheri\OneDrive\Documents\My Games\FarmingSimulator2025\log.txt`
- Log last modified: `2026-08-06 22:37:08 +08:00`
- Log SHA-256:
  `9480B91770DCB35AF90F2C22923E720D1C2679BDCC4C4F38E7FF51E1C6DDB0AE`
- Total inspected lines: `2121`
- FORGE mod discovery: line 221
- FORGE mod loading: line 556
- Development activation and suite: lines 557 and 1238–1365
- Production startup: lines 1366–1368
- Production shutdown: lines 1689–1690

Line numbers refer to the preserved log inspected on the verification date.
FS25 was exited normally without relaunching before the log was inspected.

## Development Bootstrap and Suite

- Line 557: `FORGE M2.004 development verification mode enabled`.
- Line 1238: `FORGE development test suite started`.
- Line 1365: `FORGE development test suite passed`.

This proves that the repository-controlled bootstrap enabled the existing
Logger development mode before Engine invoked the complete loaded manual
suite.

The log contains no occurrence of:

- `Participant order was not deterministic`;
- `Integrated registration lifecycle did not start`;
- `Divide by zero`;
- `LUA call stack`; or
- `FORGE development test suite completed with one or more reported failures`.

## Coordinator Regression Verification

### Registration Coordinator component harness

- Line 1330: harness started.
- Line 1331: registration opened.
- Line 1332: coordinated registration reached registration-frozen.
- Lines 1333–1337: cleanup and restarted negative-path lifecycles reached
  stopped states.
- Line 1338: harness passed.

The passing result verifies that the real production Device Registry
participated without disturbing the coordinator-owned Device, Device Host,
then App validation and freeze ordering. It also verifies participant
completeness, gating, failure handling, cleanup, shutdown, and restart.

### Registration lifecycle integration harness

- Line 1339: integration harness started.
- Lines 1340–1343: the first lifecycle opened registration, froze, became
  runtime-active, and stopped.
- Lines 1344–1347: the restarted lifecycle repeated that sequence.
- Line 1348: integration harness passed.

No cascading lifecycle-start failure occurred.

## Device Registry Capability Verification

### Component harness

- Line 1349: Device Registry harness started.
- Line 1350: its lifecycle opened registration.
- Line 1351: the intentional `DEVICE_REGISTERED` listener failed.
- Lines 1353–1355: cleanup and restart lifecycles reached stopped states.
- Line 1356: Device Registry harness passed.

The passing harness verifies:

- argument and schema validation;
- non-finite numeric rejection using a safe runtime value;
- deterministic validation-result order;
- atomic registration;
- duplicate rejection;
- detached storage and query results;
- lexical identifier ordering;
- `DEVICE_REGISTERED` timing and payload;
- listener-failure isolation after commit;
- participant validation and freeze;
- late-registration rejection;
- cleanup; and
- restart.

No divide-by-zero diagnostic or Lua stack trace occurred.

### Device Registry integration harness

- Line 1357: integration harness started.
- Line 1358: registration opened after the harness confirmed that the
  production Device Registry participant was already installed.
- Lines 1359–1361: explicit deferred-role test participants allowed
  registration freeze, runtime activation, and clean shutdown.
- Lines 1362–1363: the restarted lifecycle opened registration and stopped.
- Line 1364: integration harness passed.

This verifies production Device Registry installation before
`REGISTRATION_OPENED`, coordinator participation, cleanup, and restart.
Device Host Registry and App Registry roles were represented only by explicit
test participants.

## Production Integration Verification

### Startup

- Line 1366: production ForgeOS opened registration.
- Line 1367: FORGE persistence loaded successfully from `savegame2`.
- Line 1368: Engine startup completed.

Production registration opening occurs only after the production Device
Registry participant installation step has succeeded. No production
registration-frozen or runtime-active diagnostic appears between line 1366 and
production shutdown.

Production therefore remained correctly at `REGISTRATION_OPEN`, awaiting the
deferred concrete Device Host Registry and App Registry participants.

### Shutdown

- Line 1689: production ForgeOS reached `STOPPED`.
- Line 1690: Engine shutdown completed afterward.

The successful ForgeOS shutdown path performs Registration Coordinator
participant cleanup before publishing the stopped diagnostic. Together with
the passing component and integration cleanup assertions, this supports Device
Registry cleanup for the verified lifecycle.

Engine reported clearing zero event groups, two State Store namespaces, and
two persistence registrations.

## Intentional Negative-Path Diagnostics

The development suite deliberately exercises failure contracts. Relevant
examples include:

- lines 1247–1248: Logger fallback and fatal-level diagnostics;
- lines 1251 and 1256: intentional Event Bus listener failures;
- lines 1265–1269: invalid State Store inputs;
- lines 1277–1284: invalid and unsupported persistence inputs;
- lines 1292–1297: invalid or deliberately missing XML inputs;
- lines 1304–1306: unsupported persistence version and invalid directories;
- line 1316: intentionally rejected ForgeOS phase transition; and
- line 1351: intentional Device Registry event-listener failure.

These diagnostics occur within named negative-path harnesses and are followed
by passing component, integration, and suite-level results. They are not
production Device Registry failures.

No unexpected ForgeOS Lua error, Lua call stack, coordinator-ordering failure,
cascading lifecycle-start failure, or production startup/shutdown failure
appears in the inspected runtime evidence.

## Verification Scope

This record verifies:

- deterministic development-suite activation;
- the complete development-suite result;
- the M2.004 public Device Registry facade;
- approved device-definition validation;
- controlled plain-data validation;
- lifecycle-local identifier uniqueness;
- atomic registration and result ordering;
- detached controlled storage;
- detached and deterministic queries;
- successful `DEVICE_REGISTERED` publication;
- listener-failure isolation;
- Device Registry Registration Participant behaviour;
- production participant installation before registration-open observers;
- coordinator validation and freeze integration;
- registration freeze and runtime activation using explicit deferred-role test
  participants;
- Device Registry cleanup and restart;
- production registration-open integration;
- persistence loading; and
- production shutdown ordering.

## Explicitly Out of Scope

This record does not verify:

- a concrete Device Host Registry;
- a concrete App Registry;
- production registration freeze or runtime activation;
- production `STARTED` publication;
- device-host binding or runtime host instances;
- automatic built-in phone or laptop profile registration;
- third-party device namespace ownership or allocation;
- policy or metadata semantics;
- application registration or execution;
- presentation resolution;
- availability, lifecycle, navigation, or notifications;
- multiplayer-specific Device Registry behaviour;
- the future Diagnostics and Runtime Profiles capability;
- beta or operational bootstrap exclusion;
- M2.004 milestone acceptance or contract freeze;
- the overall M2 milestone; or
- later ForgeOS milestones.

## Verification Conclusion

M2.004 is Verified within its approved Device Registry scope.

The corrected full development suite passed. The Device Registry component and
integration harnesses passed, production installation preceded registration
opening, production remained safely at `REGISTRATION_OPEN`, persistence loaded,
Engine startup completed, and ForgeOS reached `STOPPED` before Engine shutdown
completed.

This conclusion does not implement or verify deferred concrete registries,
does not activate production runtime, does not remove the temporary
development-verification bootstrap, and does not itself accept or freeze the
M2.004 milestone.
